import { DataFormattedPipe } from './data-formatted.pipe';

describe('DataFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
